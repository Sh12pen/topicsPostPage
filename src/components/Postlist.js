import { useState } from "react";
import Post from "./Post";
function Postlist(){
    const [posts,setposts]=React.useState([]);
    function addpost(event){
        event.preventDefault=newPostInpt.value;
        setposts([posts,newpost]);}
    return(
        < {()}/>
    );    
}
